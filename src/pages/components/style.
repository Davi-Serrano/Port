import styled from "styled-components"

export const Container = styled.div`
    display: flex;
    justify-content: space-between;
    align-items: center;
    backgournd: #000000;
    
    .left, .right{
        margin: 1em
        padding: 0 1em;
    }
`
